# COMEÇO GUIADO
# Dados de partida: API, `requirements.txt`, modelo mock e fixture; use uma imagem sem serviço externo como fallback local.
# Use somente esses dados ou os valores já declarados neste arquivo. Resolva uma tarefa
# por vez, execute e confira a saída antes de seguir para a próxima.

# Docker
#
# Arquivo: dia-098-docker.py
# Arquivo preparado a partir do roadmap.
# Nenhuma atividade foi resolvida neste arquivo.
#
# NÚCLEO ESSENCIAL
# 1. [ ] Crie `Dockerfile` para a API com imagem enxuta, usuário não root quando possível e dependências fixadas.
# 2. [ ] Crie `.dockerignore` e não copie dados sensíveis.
# 3. [ ] Construa imagem, execute container e teste endpoints.
# PRÁTICA OBRIGATÓRIA
# 4. [ ] Execute o container com MODEL_VERSION=teste e confirme que /model-info mostra a configuração recebida.
# 5. [ ] Inicie sem o arquivo do modelo e faça a aplicação informar claramente a dependência ausente.
#
# Escreva sua solução abaixo desta linha.
