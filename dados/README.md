# Dados compartilhados

Esta pasta está preparada para o kit de dados citado no roadmap.

Os arquivos de dados não foram gerados nem preenchidos, pois esta etapa organiza o ambiente e não resolve as atividades. Preserve originais em `raw/` e grave versões tratadas em `processed/`.
