[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "assistente-suporte-ia"
version = "0.1.0"
description = "Projeto didático local de miniaplicação e RAG para suporte"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
